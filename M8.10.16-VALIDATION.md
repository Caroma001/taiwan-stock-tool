# M8.10.17 Validation

- Version identifiers: 8.10.17 / M8.10.17
- Unified source: `active_development_job` + `cloud_update_jobs` + `daily_update_pipeline_state` joined by active pointer.
- No second cloud-status lookup in `readDevelopmentUpdateStatus()`.
- Browser status cache key bumped to prevent stale 0/0 reuse after upgrade.
- Read-budget guard updated for Unified Progress Source of Truth.
