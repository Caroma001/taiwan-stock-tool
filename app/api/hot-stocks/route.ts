import { NextRequest, NextResponse } from "next/server";
import { addHotStock, listHotStocks, removeHotStock, updateHotStockPosition } from "@/lib/hot-stocks/service";
export const runtime="nodejs";export const dynamic="force-dynamic";
const num=(v:unknown)=>v==null||v===''?null:Number(v);
export async function GET(){try{return NextResponse.json({ok:true,rows:await listHotStocks()});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:String(error)},{status:500});}}
export async function POST(request:NextRequest){try{const b=await request.json();return NextResponse.json({ok:true,row:await addHotStock({symbol:String(b.symbol??''),reason:b.reason==null?undefined:String(b.reason),positionType:String(b.positionType??'watch'),averageCost:num(b.averageCost),quantity:num(b.quantity),purchaseDate:b.purchaseDate?String(b.purchaseDate):null})});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:String(error)},{status:400});}}
export async function PATCH(request:NextRequest){try{const b=await request.json();return NextResponse.json({ok:true,row:await updateHotStockPosition({symbol:String(b.symbol??''),positionType:String(b.positionType??'watch'),averageCost:num(b.averageCost),quantity:num(b.quantity),purchaseDate:b.purchaseDate?String(b.purchaseDate):null})});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:String(error)},{status:400});}}
export async function DELETE(request:NextRequest){try{const symbol=request.nextUrl.searchParams.get('symbol')??'';return NextResponse.json({ok:true,row:await removeHotStock(symbol)});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:String(error)},{status:400});}}
