import { NextRequest,NextResponse } from "next/server";
import { TursoDatabaseAdapter } from "@/adapters/turso";
import { getTursoClient } from "@/lib/turso";
import { MigrationRunner } from "@/migrations/database/MigrationRunner";
import { tursoMigrations } from "@/migrations/turso";
export const runtime="nodejs";export const dynamic="force-dynamic";
const num=(v:unknown)=>v==null||v===''?null:Number(v);
export async function POST(req:NextRequest){try{const body=await req.json();const rows=Array.isArray(body.rows)?body.rows:[];const db=new TursoDatabaseAdapter(getTursoClient());await new MigrationRunner(db,tursoMigrations).migrate();const now=new Date().toISOString();for(const r of rows){const symbol=String(r.symbol??'').trim();if(!symbol)continue;await db.execute({sql:`INSERT INTO ownership_structure_latest(symbol,data_date,foreign_holding_pct,foreign_holding_change,trust_5,trust_10,trust_20,large_holder_pct,large_holder_change,retail_proxy_pct,retail_proxy_change,shareholder_count,shareholder_count_change,ownership_score,capital_stage,tags_json,reasons_json,calculated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(symbol) DO UPDATE SET data_date=excluded.data_date,foreign_holding_pct=excluded.foreign_holding_pct,foreign_holding_change=excluded.foreign_holding_change,trust_5=excluded.trust_5,trust_10=excluded.trust_10,trust_20=excluded.trust_20,large_holder_pct=excluded.large_holder_pct,large_holder_change=excluded.large_holder_change,retail_proxy_pct=excluded.retail_proxy_pct,retail_proxy_change=excluded.retail_proxy_change,shareholder_count=excluded.shareholder_count,shareholder_count_change=excluded.shareholder_count_change,ownership_score=excluded.ownership_score,capital_stage=excluded.capital_stage,tags_json=excluded.tags_json,reasons_json=excluded.reasons_json,calculated_at=excluded.calculated_at`,args:[symbol,String(r.dataDate??now.slice(0,10)),num(r.foreignHoldingPct),num(r.foreignHoldingChange),num(r.trust5),num(r.trust10),num(r.trust20),num(r.largeHolderPct),num(r.largeHolderChange),num(r.retailProxyPct),num(r.retailProxyChange),num(r.shareholderCount),num(r.shareholderCountChange),Number(r.ownershipScore??0),String(r.capitalStage??'資料不足'),JSON.stringify(r.tags??[]),JSON.stringify(r.reasons??[]),now]});}return NextResponse.json({ok:true,imported:rows.length});}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:String(error)},{status:400});}}
