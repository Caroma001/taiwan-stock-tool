"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginPage(){const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});if(!r.ok){setError("密碼錯誤");return;}const next=new URLSearchParams(window.location.search).get("next")||"/daily-lab";router.replace(next);router.refresh();}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",background:"#020617",color:"#e2e8f0",padding:24}}><form onSubmit={submit} style={{width:"min(420px,100%)",background:"#0f172a",border:"1px solid #334155",borderRadius:16,padding:28}}><h1>Bruce 台股決策中心</h1><p style={{color:"#94a3b8"}}>請輸入私人存取密碼</p><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} style={{width:"100%",padding:12,borderRadius:8,border:"1px solid #475569",background:"#020617",color:"white",boxSizing:"border-box"}}/>{error&&<p style={{color:"#fb7185"}}>{error}</p>}<button style={{marginTop:16,width:"100%",padding:12,border:0,borderRadius:8,background:"#2563eb",color:"white",fontWeight:800}}>登入</button></form></main>}
