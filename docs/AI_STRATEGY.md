# AI Strategy Release Rule

M8.7.1 does not change AI scoring or portfolio recommendations. It provides a repeatable release framework so later scoring changes can be verified, versioned, deployed, and rolled back without mixing deployment errors with model changes.
